#include <iostream>
using namespace std;
class Student
  
{string name;
 public:
  
  Student (string name)
  {cin>>name;
    cout<<"Hello "<<name;
  }
  Student()
  {
    cout<<"No Attendance\n";
  }
  student(string name,int age)
  {cout<<name;
   cout<<age;
  }
};
int main()
{string name;
  Student stdabs;
 Student stdpst (name);
 Student std(name,age);
 //stdabs.Student();
 //stdpst.Student(name);
  return 0;
}
